# Local configuration — ALWAYS created by the user from
# cordis.patch.yml.example. Never commit: it carries your board ids,
# session ids and local paths.

# Local agent-tooling state (never publish)
.claude/
.omc/
.remember/

node_modules/
*.log
.DS_Store
